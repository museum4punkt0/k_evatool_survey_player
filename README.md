# evaluation-tool-survey-player

yarn install
yarn dev
yarn build
yarn electron:dev
yarn electron:build


* Alt+CommandOrControl+O to set the url
* ELECTRON_FULLSCREEN=1 can be changed at .env.local

